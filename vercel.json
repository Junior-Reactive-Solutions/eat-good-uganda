{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @eatgood/shared build && pnpm --filter @eatgood/customer build:ci",
  "outputDirectory": "apps/customer/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
